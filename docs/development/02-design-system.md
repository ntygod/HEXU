# 02｜设计系统、应用外壳与核心页面骨架

> D1 · M0—M3 · 关联 HX-F04/F09/F10 · 依赖：01 的目录与契约。  
> [计划入口](README.md) · [UI 产品目标](../product/04-ux-and-design.md) · [任务工作区](10-task-workspace.md)

## 1. 交付目标与范围

将产品 v1.1 和已确认的 [Workbench W1](../design/README.md) 转成可复用组件与页面结构。工作台、项目看板、任务协作、继续面板、并行对比和成果反馈使用同一设计语言。W1 的 tokens 与交互参考已归档，现有应用尚未整体迁移；参考图不代表真实业务能力已经存在。

管理页和开发页共享事实但密度不同。导航只有工作台、项目、成果和资源设置；任务从项目或搜索直接进入，不再建一个同级的 AI 工作区。

## 2. 页面与路由草案

| 路由 | 页面 | 主动作 |
| --- | --- | --- |
| `/workbench` | 我的工作 / 团队概览 | 继续任务、回复、看成果 |
| `/projects/:projectId` | 项目总览与资料 | 新建任务、看板、成员 |
| `/tasks/:taskId` | 任务研发工作区 | 继续、请人或 AI 协助、并行 |
| `/tasks/:taskId/compare` | 方案对比 | 选择方案继续、查看差异 |
| `/results/:resultId` | 成果与反馈 | 提出修改、继续处理、标记完成 |
| `/settings/resources` | 工具、模型、节点、集成 | 接入和维护 |

同一路由记录所属 space，通过访问判断加载；直接分享 URL 不等于授权。个人探索在标题附近明确“仅自己可见”。

## 3. 组件与视觉实现

视觉以 W1 的暗色优先、青色强调、完整浅色主题、紧凑／舒适密度为准。数值见 [设计 tokens](../design/tokens.css)，新组件逐步迁移到 `packages/ui` 的共享实现，不在业务页复制另一套配色。参考 tokens 尚未被应用导入，旧 `packages/ui/src/tokens.css` 的靛蓝色不再代表新的设计选择。

建立 `AppShell`、`ProjectHeader`、`TaskRow`、`TaskStatusBadge`、`RunStatusBadge`、`CodeStatus`、`PromptBar`、`Participant`、`ContextReference`、`ExecutionEvent`、`ResultCard`、`FeedbackThread`、`CapabilitySelect`、`ActionDrawer` 和 `EmptyState`。TaskStatus、RunStatus 与代码事实分别表达，不能用一个 status pill 隐藏区别。桌面和 Web 共用这些组件与业务契约，见 [ADR-0008](../engineering/adr-0008-client-surfaces.md)。

人显示头像和姓名，AI 显示工具名与配置。先用可合法使用的文字/图标占位；第三方 Logo 的来源需登记，不能复制来源不明的图片资产。中文优先，代码等宽，紧凑正文 13px、舒适 14px，次要标签 12px；必要内容不为塞入多栏而缩小。长标题和译文自然换行。

## 4. 关键布局

任务页采用顶栏、约 48px 导航轨、可收起的 180–220px 导引栏、过程与结果双栏、稳定输入区和按需旁路抽屉。约 1024px 优先收起辅助区域，约 768px 以下切换或堆叠内容，重点支持查看和回复；不强行常驻全部面板。项目、团队、成果页保持阅读布局。断点按实际内容校验。

工作台第一屏应有继续工作与待回复；不放大幅宣传山景、成员在线排名、无依据的进度百分比。成果可以在任务未完成时出现，不加“必须验收”的绿色进度条。

继续、协助、并行使用相邻但可区分的动作。打开操作抽屉保留原任务位置，返回恢复焦点。没有配置时显示设置引导；不支持的能力说明原因，而不是展示可点击但无效的按钮。

## 5. 状态与交互细节

统一覆盖普通、加载、空、断线、权限不足、失败、草稿未发送、数据更新冲突等状态。用户输入失败不清空；多人编辑冲突保留本地草稿并展示服务器修订。

协作消息只在用户接近底部时自动滚动，阅读历史时给出“有新消息”；长日志按块虚拟化，不让单条输出撑垮页面。状态变化用简短文本而非持续动画。

键盘可操作看板菜单、切换任务、搜索、回复和关闭弹层；不能只有拖拽路径。用文本和图标同时表达状态，尊重减少动态效果设置。

## 6. 详细工作项

| ID | 工作内容 | 交付物 | 依赖 |
| --- | --- | --- | --- |
| HX-DEV-02-01 | 将视觉方向整理成浅/深色、间距、字体和状态 tokens | ui/tokens、设计变量说明 | 01-02 |
| HX-DEV-02-02 | 实现全局导航、空间切换、路由外壳、项目快捷入口 | AppShell、route layouts | 02-01 |
| HX-DEV-02-03 | 实现任务、运行、人物/工具、成果与上下文组件 | 组件目录与状态示例 | 02-01、01-03 |
| HX-DEV-02-04 | 制作工作台、项目、任务、成果四类页面骨架并接模拟数据 | 可交互演示路由 | 02-02、02-03、01-05 |
| HX-DEV-02-05 | 完成继续/协助抽屉、并行选择面板、反馈与回复组件 | ActionDrawer 与业务插槽 | 02-03 |
| HX-DEV-02-06 | 补齐响应式、键盘路径、空/错/离线、布局偏好和文案字典 | 通用体验与组件使用说明 | 02-04、02-05 |

## 7. 交付给后续计划的边界

02 不实现任务权限判断和 Agent 调度，只消费服务提供的 `availableActions` 与能力描述。10、11、14 负责真实行为接入。模拟按钮只能改变模拟环境，不访问真实节点。

设计交付包含组件与页面，不只一张整屏海报。关键页面可以先用假数据实现视觉，但必须标注“演示数据”；生产文案不写“系统验收通过”。
