# 22｜下一步产品交付

更新：2026-09-27（UTC+8）。当前 **E2c2 + W1 + 项目生命周期、任务改派/参与者与人员筛选**；原编号、状态和历史以 [19](19-work-items.md) / [21](21-implementation-status.md) 为准。本页是唯一下一步计划，不另建任务系统。

## 当前接手结果

W1-01—09 已完成，不重启 UI 重建。`HX-DEV-08-03` 的有界 Claude 显式恢复代码路径已接入：本机 opt-in、私有历史和 UUID、绑定/文件指纹、原生 init/result 核对、失败不自动新建、W1 任务入口及本机清理。Codex 原路径保留。相关完整工作项仍为部分实现，**真实账户成功生成/恢复未联调**，不能由协议替身、官方无模型检查或截图代替。

部署仍是 preview / team-local 同机回环，节点只允许本人单独授权执行。桌面优先与可选团队服务是产品方向，不是已完成的安装包、跨电脑部署或 Electron/Tauri 决策。

## 下一主线：05-02 项目资料与修订

项目基本设置、03-06 本机归档/恢复以及 04-01 项目任务真人改派已接入。改派保留创建者/执行发起者，历史、任务修订与等待接续暂停同事务保存；不改变活动 Run、代码目录、节点授权、账户或冻结材料。私有任务不会通过改派公开。实际检查以 [21](21-implementation-status.md) 为准，不重复已完成切片。

04-01 参与关系与 04-03 人员筛选也已接入：本人加入/退出、编辑者管理当前项目成员、独立修订和历史、项目/空间撤权结束关系；看板/列表/API 共用负责人、参与者和关键词交集，URL 保存视图与条件。参与不授予访问或执行权，不属于已确认的模型材料，不修改 Task/Run/Operation；不要重复实现。完整状态/标签/attention 筛选、持久排序和私有选择性共享仍单列待交付。

下一切片按 [05 工作包](05-context-discussion.md) 实现项目资料，先交付人工文本资料/链接引用、稳定资源 ID、修订、可解释删除范围和 W1 项目入口。沿用当前项目 read/edit/manage 边界及同事务历史/outbox/幂等，不把链接保存变成任意网络抓取，不把文件路径变成 Runner 授权。附件上传/存储和仓库连接按各自实际实现记录，不能用文本引用宣称这些能力已完成。

资料有了版本和权限后，再推进 05-03 从讨论保存约定和 05-05/06 按目的选材与上下文面板。先权限过滤，再确定版本/来源/哈希与预算；保存资料不等于模型收到，更不能静默改写当前 Run 或已冻结 Operation。先提供确定性拼装与人工摘要，AI 总结不能成为使用前提。真实发送时仍要复核权限，复用已有执行/接续规则。

继续使用 W1 当前项目/任务工作区，不重建全套 UI。正式存储、跨电脑部署与桌面宿主按既有 ADR 单独交付；协助、接手和隔离并行在资料/上下文基础后推进。

每批实现一个可用切片并更新同一 19/21/22，保留实际平台、回归和剩余范围；内部评估不变成产品强制验收。

## 保留的适配器工作

真实授权账号下的 Claude/Codex 生成、已有历史恢复、费用范围、权限和预算效果单独记录为未验证；没有账号不得自行借用开发者密钥。只在获得明确测试授权的环境联调，结果回写 [21](21-implementation-status.md)。

`08-04 / 09-03 / 07-04/05` 的原生即时输入、澄清和动作授权仍未实现；现有下一轮要求不能改名为 delivered。失败/取消历史重用、账户轮换迁移、自动等待原生恢复不纳入本轮默认行为，继续保持明确拒绝。官方无模型兼容性可用 `check:claude-protocol` / `check:codex-protocol` 复核，检查不到的版本不虚构支持。

## 后续顺序与不变边界

项目基础与任务负责人/参与者切片后继续项目资料与上下文，再按原计划推进协助、接手和隔离并行。正式存储、跨电脑安全传输与个人桌面宿主按既有 ADR 拆分，不在一个 PR 同时重写 UI、协议和数据库。

所有流程复用 Task / Run / Operation；恢复创建新 Run，Task.done 仍由用户决定，Operation.succeeded 仅代表后续 Run 原子创建。未知进程保锁，不执行自动 reset/stash/commit/push。默认新会话，保留历史需要本机确认；恢复失败不能切账号、降权限或重试付费执行。每批至少完成一个可运行切片并如实记录实际测试平台、失败和剩余范围。
