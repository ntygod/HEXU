# 22｜下一步产品交付

更新：2026-09-26（UTC+8）。当前 **E2c2 + W1 + 项目设置与归档**；原编号、状态和历史以 [19](19-work-items.md) / [21](21-implementation-status.md) 为准。本页是唯一下一步计划，不另建任务系统。

## 当前接手结果

W1-01—09 已完成，不重启 UI 重建。`HX-DEV-08-03` 的有界 Claude 显式恢复代码路径已接入：本机 opt-in、私有历史和 UUID、绑定/文件指纹、原生 init/result 核对、失败不自动新建、W1 任务入口及本机清理。Codex 原路径保留。相关完整工作项仍为部分实现，**真实账户成功生成/恢复未联调**，不能由协议替身、官方无模型检查或截图代替。

部署仍是 preview / team-local 同机回环，节点只允许本人单独授权执行。桌面优先与可选团队服务是产品方向，不是已完成的安装包、跨电脑部署或 Electron/Tauri 决策。

## 下一主线：04-01 任务负责人和参与者

继续原 `HX-DEV-04-01`，联动 `03-03/06、04-03/06、05、10`。项目名称/说明设置和 `03-06` 本机归档/恢复已接入，不重复做这两批。归档暂停新执行、原子暂停旧等待安排、取消未许可派发，并提供已运行工作的 keep/stop；恢复不自动重放，也不改变任务和节点权限。实际检查以 [21](21-implementation-status.md) 为准。

第一切片为项目任务的真人负责人修订式改派与显示。候选人来自当前有权限的真实成员，创建者、负责人、运行发起者分别保留；改派不能转移目录、模型账户、节点授权或旧 Run 的原生历史，不能自动停止运行或发起付费执行。私有任务不能借改派隐式公开；初始范围可明确仅项目可见任务，跨空间/私有转共享另行交付。

接着补参与者关系、退出/撤权后的可解释显示和列表筛选。沿用同一 Task DTO、PermissionService、项目成员与修订/幂等/outbox事务；不要建立 AI 员工层级、评分或单独会话任务库。负责人不是额外访问授权，也不能让节点所有者权限变成所有项目成员可执行。

实施前核对改派对活动 Run 和冻结接续材料的影响：记录真实身份与修订，不静默改写已确认材料；需要暂停的安排保留原要求，不自动重新派发。UI 在现有 W1 任务头部/详情内扩展，不另造任务配置页面或恢复已删除旧布局。

仓库引用仍未接入；未来只保存显式填写的元信息，粘贴地址不能触发克隆、安装或运行脚本。正式部署、跨电脑传输和个人桌面宿主按现有 ADR 单独交付。

每批实现一个可用切片并更新同一 19/21/22，保留实际平台、回归和剩余范围；内部评估不变成产品强制验收。

## 保留的适配器工作

真实授权账号下的 Claude/Codex 生成、已有历史恢复、费用范围、权限和预算效果单独记录为未验证；没有账号不得自行借用开发者密钥。只在获得明确测试授权的环境联调，结果回写 [21](21-implementation-status.md)。

`08-04 / 09-03 / 07-04/05` 的原生即时输入、澄清和动作授权仍未实现；现有下一轮要求不能改名为 delivered。失败/取消历史重用、账户轮换迁移、自动等待原生恢复不纳入本轮默认行为，继续保持明确拒绝。官方无模型兼容性可用 `check:claude-protocol` / `check:codex-protocol` 复核，检查不到的版本不虚构支持。

## 后续顺序与不变边界

项目基础之后继续任务负责人/参与者、项目资料与上下文，再按原计划推进协助、接手和隔离并行。正式存储、跨电脑安全传输与个人桌面宿主按既有 ADR 拆分，不在一个 PR 同时重写 UI、协议和数据库。

所有流程复用 Task / Run / Operation；恢复创建新 Run，Task.done 仍由用户决定，Operation.succeeded 仅代表后续 Run 原子创建。未知进程保锁，不执行自动 reset/stash/commit/push。默认新会话，保留历史需要本机确认；恢复失败不能切账号、降权限或重试付费执行。每批至少完成一个可运行切片并如实记录实际测试平台、失败和剩余范围。
