# 22｜下一步交付：补齐双工具会话，再推进团队基础

更新：2026-09-26（UTC+8）。E2c1 推进 Codex 私有会话与显式 resume；不要再用等待按钮或复制历史文本声称新的原生能力。

## 下一主线：08-03 Claude 原生会话映射

复用本轮“默认不保留、节点私有历史、同任务/目录/工具/账户、明确恢复、失败无静默新建”的边界，核对 Claude 当前 CLI 的实际会话存储和恢复参数。先完成 CLI 版本能力、持久会话目录、启动/恢复身份核对与界面分流；不把 Codex 字段机械照搬给 Claude。

目前缺有效账户生成结果，两种工具的真实账户互操作仍单列未验证。可以继续实现协议与状态，但发布声明必须区分“代码路径/替身回归”“官方无模型检查”“有效账户实际恢复”。不向公共 CI、仓库或任务粘贴 API key，不复用个人订阅身份。

## 持续使用和产品体验

原生恢复只对已成功结束的最新来源开放。失败/中断会话恢复与自动等待原生 resume 需要确认原 turn 状态、历史范围和取消窗口，不能简单给既有 Operation 添加一个 ID。优先减少误导、显示明确失败原因及保留的要求，不引入强制内部验收环节。

## 团队基础仍要单独推进

01-04 正式 PostgreSQL 与 17 HTTPS/远程初始化、设备传输、备份迁移保留原 Task/Run/Operation ID 和权限。03 的邮件验证/找回密码、项目编辑归档和成员角色仍未收口；不能单独开放 0.0.0.0 来称为部署完成。

指定他人使用节点需本机有限授权；临时协助需要所选快照与 AccessGrant；接手需要不可变代码检查点；并行需要独立工作区。本轮原生私有历史不能公开替代上述材料和权限。
