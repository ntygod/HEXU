# 22｜下一步交付：原生会话与提供方兼容性

更新：2026-09-26（UTC+8）。E2b4 已补齐独立节点的持久化 wait/request_stop、取消与恢复。下一批不再扩展同一套等待按钮，转向原工具会话能力。

## 下一主线：08-03 / 09-02 原生会话映射

现有接续明确是新会话，只有文件和公开材料连续。下一步按官方实际协议核对 Claude/Codex 的会话创建、引用、恢复、账户绑定与保留期限，将可恢复状态与仅可查看历史分开。不能只保存 session ID，或用协议替身输出声称真实原生 resume 可用。

先选择一个能够在既有受限文件工具和明确账号边界内恢复的适配器交付纵向路径：节点私有会话存储、同任务/目录/工具/账户核对、用户选择新会话或继续原会话、真实引用和失败回退。恢复失败不能静默重启另一场付费工作；跨工具依旧新会话，不迁移模型内部状态。

有效账户下的模型生成需要授权的非敏感试验仓库，当前仍未联调。没有账户结果就保留未验证，不把 Key 写入仓库、公共 CI 或任务。协议能力检查不等同于模型调用成功。

## 并行基础工作

01-04 的 PostgreSQL/备份迁移和 17 的 HTTPS/远程部署应保留同一 Task/Run/Operation 模型，不重建另一套团队系统。逐步替换 SQLite 前明确认证与业务数据恢复、原 ID、历史记录和敏感本地上下文的导入范围。

跨电脑接入必须一并处理设备认证、来源、传输、代次、重放与恢复，不单独放开监听地址。03 的邮箱验证/找回密码、项目编辑归档和成员管理仍按原清单补齐。

## 协助、接手、并行的进入条件

指定他人使用节点需要本机明确授予主体/项目/目录/工具/预算边界；项目角色不是节点使用权。11-03/04 协助需选中快照和有限 AccessGrant；12 接手需不可变检查点与目标恢复；13 并行写入需独立现场。当前固定文字快照和 Git 数量摘要都不能替代这些能力。

UI 随每个纵向功能一起交付；内部质量与效果评估不转换为产品内强制审批或验收流程。
