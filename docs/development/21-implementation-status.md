# 21｜当前实现进度

更新：2026-09-26（UTC+8）。阶段：**E2b3 下一轮要求与独立节点同目录接续**。产品 v1.1 / D1 完整目标不变。

## 原任务位置

推进 `07-04、11-01/02/05`，联动 `05-05、06-06、10-03`。原 102 项仍为 **2 项已完成、61 项部分实现、39 项未实现**。已有部分实现继续补齐，不把整包或原生恢复提前标完成。

## 本轮实际交付

| 能力 | 实际范围 |
| --- | --- |
| 下一轮要求 | 项目编辑成员可在节点任务中保存；本人编辑/撤回；最多 20 条待处理，每条 2000 字符；刷新和服务重启保留 |
| 输入真实语义 | POST /runs/:id/inputs 对 node 返回 queued_for_next_turn；不送入活动进程、不停止它，不自动开下一轮，不伪造 delivered |
| 接续预览 | 明确源 Run、源工具、固定节点/目录，当前任务/近期人工讨论、来源实际共享输出；不会采用终态后排空的迟到输出 |
| 同目录新会话 | 仅源 Run 已确认结束、最新来源、本人节点仍授权且目录相同时派发；保留未提交修改、previousRunId 和选中要求，沿用原 Task/Run |
| 选择与原子性 | 默认不全选；最多选择 6 条、选材总量有界；校验材料哈希和要求修订，Run/dispatch/关联/要求绑定/幂等/outbox 一起提交或回滚 |
| 使用状态 | 待选→绑定派发→随实际 spawn 启动；启动并不代表模型已理解或完成。明确未发许可的取消会退回待选；有启动歧义时保留绑定，避免自动重发 |
| UI | 任务内的下一轮工作区、编辑/撤回、来源说明、选材、完整发送文本、清楚的重开动作与浅深色窄屏布局 |
| 本机诊断 | pending-executions 展示派发/任务/目录 ID、日志阶段与待 ACK 数；不输出提示词或凭证，不改变状态，不推断旧 PID 已停止 |

新保存的下一轮要求不会混入已经排队的命令，也不会因保存它而取消当前派发。任务说明或人工讨论变化依然走启动前的材料校验。项目只读成员可查看队列但不能写；撤销访问后的直接 ID、列表和旧请求重放均重新检查权限。

## 仍未交付

本轮节点接续是**明确点击后的 201+Run 新会话**，不是 preview 的自动等待接续 Operation；活动执行须先结束或确认停止，然后重新选择继续。不会恢复 Claude/Codex 原生会话或迁移模型内部状态。工具来自节点当前的单一执行授权；切换工具要在节点本机重新 enable-execution 并发布，网页不能代换路径、Key 或预算。

仍仅支持同机回环上的独立节点本人执行。跨电脑 TLS/WSS、PostgreSQL、系统凭证存储、Windows、指定他人执行、完整 AccessGrant、原生 resume/steer/实时输入、自动等待节点接续、远程 diff/终端/预览、临时协助、跨成员检查点接手、并行分支未完成。

**有效提供方账户下真实模型生成仍未联调。** 本轮工具流程使用协议替身、虚构 Key、实际 HTTP/SQLite/进程/临时 Git 目录。文件工具策略不是 OS 沙箱；输出脱敏不是通用数据防泄漏系统。

## 工程记录

本地 Linux / Node 24.21.0：151 条工程测试通过，0 失败、0 跳过；类型检查、格式检查及构建已通过。

[E2b3 完整开发检查 36220689405](https://github.com/ntygod/HEXU/actions/runs/36220689405) 已在 GitHub-hosted Ubuntu / Node 24 通过。工作流应用并格式化后的实际检查源码为 `9e6d5ad394777539e0e929f2ea54e79a0ebe2d76`，不是触发工作流的临时传输提交。已下载源码快照，与本地应用、包、测试、脚本及文档共 156 个文件逐一核对一致（不含临时工作流和传输文件）。

| 检查 | 实际结果 |
| --- | --- |
| 锁文件安装、格式检查 | 通过 |
| 双端 TypeScript、前后端构建 | 通过 |
| 领域、存储、API、认证、原生协议替身、节点与持续工作测试 | 151 条通过，0 失败、0 跳过 |
| Chromium 浏览器流程 | 29 条通过，0 失败、0 不稳定、0 跳过 |
| 浏览器产物 | 已下载并读取报告，抽查接续完成和手机深色下一轮要求页面 |

新增 3 条浏览器流程实际完成运行中保存下一轮要求、刷新、明确停止后沿原目录继续、只带入所选要求、编辑/撤回、材料变化重新确认，以及已完成任务的显式重开。既有 26 条流程保留；没有删除失败用例、跳过或重试来掩盖结果。

本地联调中修正了团队接口对 node 下一轮输入的旧限制、来源最终结果未纳入接续摘要，以及测试替身把来源历史标记误当作本轮要求的问题；修正后运行完整工程与浏览器回归。浏览器结果来自 GitHub-hosted runner，不计为本地浏览器通过。截图不代替功能断言。

主分支最终结果以对应提交的只读 CI 为准。临时源码传输文件和写入式开发工作流不进入主分支。

新增检查覆盖下一轮队列不影响当前执行、旧修订/跨任务/旧来源拒绝、未知进程保留、明确选材、事务回滚、取消退回、重复派发、实际输出与脏文件保留、权限/撤回和本机待处理列表。原 E2b2 的 140 条工程测试与 26 条浏览器流程保留，历史基线主分支为 `65fc778`。

## 下一步

先完善节点同目录继续的自动等待/取消与恢复体验，使其与 preview 的 Operation 语义对齐，再根据工具实际协议接入原生 resume。远程部署、正式存储和有限成员授权仍单列推进，不为了功能按钮放开监听地址。用法见 [持续工作](../engineering/node-continuation.md)，剩余范围见 [22](22-next-delivery.md)。
