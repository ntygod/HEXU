# 21｜当前实现进度

日期：2026-09-25。代码阶段：**E0 本地开发预览**。对应产品 v1.1、开发计划 D1。原工作项用于范围索引，本文件记录实际进度，不把一个演示切片当作整个工作包完成。

## 本轮实现

TypeScript monorepo；React/Vite 网页；Fastify 本地 API；独立的 contracts、domain、db、client、ui、mock；SQLite 迁移与持久化；重复请求去重；修订冲突；任务与执行状态分离；持久化事件与 SSE 更新。直接与传递依赖已经写入 package-lock.json。

工作台、项目列表、看板/列表切换、新建项目/任务、说明编辑、等待标记、评论、完成/重开/取消接口、文字成果分享、成果反馈、任务搜索、浅深色与窄屏布局已经建立真实的本地数据流程。

## 模拟与未实现

模拟执行可以开始、等待输入、等待授权、失败、结束和停止；没有读取代码目录、模型调用或代码修改。演示订单页面可筛选、分页和导出虚构 CSV；不是通用预览隧道。

未实现：真正登录和团队权限、PostgreSQL、原生 Claude Code/Codex、运行节点、工作区文件与终端、跨工具代码现场接续、临时协助、成员接手、并行分支、模板与真实集成。

示例成员不是已注册账号；模拟中的工具名称不是原生接通状态。当前程序只监听回环地址，不作为公网或生产团队服务使用。

## 工作项覆盖

| 工作包 | 本轮状态 | 尚缺什么 |
| --- | --- | --- |
| 01 工程与契约 | 部分实现 | 正式 PostgreSQL、完整节点/适配协议、正式安装组合 |
| 02 UI 系统 | 部分实现 | 已有核心组件/页面，尚缺高级协作与完整无障碍评估 |
| 03 账号项目 | 仅本地项目基础 | 多用户身份、邀请和生产权限 |
| 04 任务 | 部分实现 | 依赖、里程碑、复杂需求说明、跨团队分享 |
| 05 讨论上下文 | 部分实现 | 真实模型上下文组装、项目约定与资料检索 |
| 06 执行器 | 未实现 | 本地守护进程、配对、工作目录、进程控制 |
| 07 执行事件 | 本地模拟切片 | 真正节点派发、原生授权与恢复 |
| 08 / 09 工具适配 | 未实现 | 原生 Claude Code 与 Codex |
| 10 任务工作区 | 页面与本地数据切片 | 真实 diff、文件、终端和外部 IDE |
| 11—13 协作接手并行 | 未实现 | 本轮面板不代表代码现场可迁移 |
| 14 成果反馈 | 文字成果与示例预览 | 真实文件/代码成果、隧道、多修订编辑 |
| 15 工作台搜索 | 部分实现 | 权限完备的团队搜索、通知偏好和真实费用 |
| 16 / 17 自动化部署 | 未实现 | 集成、远程节点和可公开使用的部署 |

没有将 102 项全部打勾。01—20 保留完整建设目标，实际实现以本页及源代码为准。

## 已执行的工程检查

[开发分支检查记录](https://github.com/ntygod/HEXU/actions/runs/36148288625)，运行环境为 GitHub-hosted Ubuntu、Node 24.21.0。该运行在 2026-09-25 完成。

| 检查 | 结果 | 范围 |
| --- | --- | --- |
| 依赖安装与锁定 | 通过 | npm install --ignore-scripts，生成实际锁文件 |
| TypeScript | 通过 | 服务端及网页的完整类型检查 |
| 领域、存储与 API | 29 条通过 | task/run 语义、持久化、幂等、修订、访问过滤、本地 API |
| 生产构建 | 通过 | TypeScript 服务端与 Vite 网页 |
| Chromium 端到端 | 10 条通过，0 跳过 | 工作台、看板、新建、评论恢复、模拟等待/停止、成果反馈、CSV、搜索、深色、窄屏与文本安全 |
| 浏览器截图 | 已生成并抽查 | 工作台、任务工作区、成果、深色及手机宽度 |

此外，在开发容器中独立运行的 24 条领域/存储测试全部通过。该容器不能直接下载 npm 依赖，因此完整安装、API、构建和浏览器检查在仓库 CI 中实际执行，而不是由文档推断。

临时 bootstrap 工作流只用于首次生成锁文件及格式化来源代码，合入主分支时移除。常规 CI 使用 npm ci、格式检查、完整检查与浏览器用例。其后运行结果以对应 Actions 记录为准。

这些结果不代表原生模型适配、多人授权、Windows/macOS、生产安全或真实业务质量已经验证。内部团队继续自行安排实际评估；不在产品中增加验收中心。

## 下一组开发重点

接入成熟身份组件和 PostgreSQL，保留已有任务、讨论与成果体验；同时建立独立执行器和适配协议。随后用真实工具替换 mock，先完成一个原生工具的单任务输入、输出、停止与状态，再接通第二个工具与真正“继续”。

当前没有读取公司模型凭证，也没有部署外网服务。后续账号、操作系统和上线范围按实际授权配置。
