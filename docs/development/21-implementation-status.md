# 21｜当前实现进度

日期：2026-09-25。代码阶段：**E0 本地开发预览**。对应产品 v1.1、开发计划 D1。原工作项仍用于范围索引，本文件记录实际进度，不把一个演示切片当作整个工作包完成。

## 本轮已写入的真实能力

TypeScript monorepo；React/Vite 网页；Fastify 本地 API；独立的 contracts、domain、db、client、ui、mock；SQLite 迁移与持久化；重复请求去重；修订冲突；任务与执行状态分离；持久化事件与 SSE 更新。

工作台、项目列表、看板/列表切换、新建项目/任务、说明编辑、等待标记、评论、完成/重开/取消、文字成果分享、成果反馈、任务搜索、浅深色与窄屏布局。

## 模拟与未实现

模拟执行可以开始、等待输入、等待授权、失败、结束和停止；没有读取目录、模型调用或代码修改。演示订单页面可筛选、分页和导出虚构 CSV；不是通用预览隧道。

未实现：真正登录和团队权限、PostgreSQL、原生 Claude Code/Codex、运行节点、工作区文件与终端、跨工具代码现场接续、临时协助、成员接手、并行分支、模板与真实集成。

## 工作项覆盖

| 工作包 | 本轮状态 | 尚缺什么 |
| --- | --- | --- |
| 01 工程与契约 | 部分实现 | 正式 PostgreSQL、完整节点/适配协议、正式安装组合 |
| 02 UI 系统 | 部分实现 | 已有核心组件/页面，尚缺高级协作与完整无障碍评估 |
| 03 账号项目 | 仅本地项目基础 | 多用户身份、邀请和生产权限 |
| 04 任务 | 部分实现 | 依赖、里程碑、复杂需求说明、跨团队分享 |
| 05 讨论上下文 | 部分实现 | 真实模型上下文组装、项目约定与资料检索 |
| 06 执行器 | 未实现 | 本地守护进程、配对、工作目录、进程控制 |
| 07 执行事件 | 本地模拟切片 | 真正节点派发、原生授权与恢复 |
| 08 / 09 工具适配 | 未实现 | 原生 Claude Code 与 Codex |
| 10 任务工作区 | 页面与本地数据切片 | 真实 diff、文件、终端和外部 IDE |
| 11—13 协作接手并行 | 未实现 | 本轮面板不代表代码现场可迁移 |
| 14 成果反馈 | 文字成果与示例预览 | 真实文件/代码成果、隧道、多修订编辑 |
| 15 工作台搜索 | 部分实现 | 权限完备的团队搜索、通知偏好和真实费用 |
| 16 / 17 自动化部署 | 未实现 | 集成、远程节点和可公开使用的部署 |

没有将 102 项全部打勾，没有创建未被实际完成的原生适配状态。

## 运行检查

本地已实际执行 24 条领域/存储测试。当前执行环境不能直接访问 npm，完整依赖安装、类型检查、API 测试、构建和浏览器检查由仓库 CI 执行；结果以对应运行记录为准。在结果返回前不把它们填写为通过。

这些检查用于开发，不建设产品验收中心。内部团队继续负责真实业务评估。

## 下一组开发重点

先接入成熟身份组件和 PostgreSQL，保持现有任务、讨论与成果用户体验；同时建立独立执行器和适配协议。随后用真实工具替换 mock，优先完成 Claude Code 或 Codex 的单任务输入、输出、停止与状态，再接通第二种工具与真正“继续”。

角色、操作系统、模型账户和上线范围按公司实际环境确定，当前不读取公司凭证，也不把本地预览当作远程服务部署。
