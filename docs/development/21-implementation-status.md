# 21｜当前实现进度

更新：2026-09-27（UTC+8）。阶段：**E2c2 双工具会话代码路径 + W1 全套界面 + 项目生命周期、任务改派/参与者与人员筛选**。产品 v1.1 / D1 不变。

## 当前增量：04-01 参与者与 04-03 共用筛选

从 PR #5 合并基线 `ad7c623` 继续。接手时恢复其已经生成但尚未推到分支的收尾提交，移除临时写工作流；最终 [只读 CI 36257267132](https://github.com/ntygod/HEXU/actions/runs/36257267132) 的 **215 工程 / 49 浏览器** 全部通过后 squash 合并。原 102 项保持 **2 完成 / 61 部分 / 39 未实现**。

- 同一 Task 增加独立修订的参与关系：本人加入/退出，编辑者管理当前项目成员，分页不可变历史与当前/此前参与状态。只读成员可自行参与，不能借此编辑任务或操作他人；私有任务不开放此入口。
- 关系/修订/历史/outbox/回执原子提交。撤销项目或空间访问同时结束参与；重新加入及历史回执不自动恢复旧关系。当前权限在读取、修改、重放及事件投递时继续检查。
- 参与关系不属于本轮模型材料，不修改 Task 修订、负责人、运行、派发、冻结接续或代码锁。保留当前已确认接续，真实撤权仍按原权限检查处理。归档允许人工协作。
- 项目看板/列表/API 共用负责人、参与者和标题/编号/说明的交集筛选。链接保存条件和视图，刷新/后退保留；人员选项限定当前项目，历史负责人只用该任务已记录姓名。详情/工作台/列表的参与者 ID 是读取投影，不写回 Task JSON。
- W1 原任务头部与抽屉接入，逐次操作、明确冲突、原请求确认和权限变化清理；无第二套任务模型。历史有独立刷新，阅读不被新事件跳动。

本地 Windows / Node 24.19.0 已通过新增 **8 项服务端测试**、双端类型及生产构建；初轮发现通用写路由阻止只读本人参与，已将例外限定为该语义端点且再次走当前父任务/参与权限，针对测试全部通过。复查将手机端隐藏规则限定到重复的上下文快捷按钮，保留参与入口；原提交 CI 因后续修正自动取消，不计为通过。使用与剩余边界见 [参与者与筛选](../engineering/task-participants.md)。

[完整只读 CI 36259533521](https://github.com/ntygod/HEXU/actions/runs/36259533521) 检查功能头 `3c1c743391cd6c12c3a010487b66119e4c589317` 与基线 `ad7c623` 的合并预览 `a9d0c2f`，实际日志确认安装、格式、双端类型、生产构建、**223/223 工程测试、53/53 Chromium 流程通过**，无失败、跳过或重试。原 215/49 全部保留，新增 8/4；浏览器覆盖本人加入/退出、其他成员管理、历史、深浅色/390px 手机、未知回执重放、陈旧修订、组合筛选刷新/返回和真实只读/撤权/重新加入。协议替身不计为真实模型联调。

已下载该轮产物 `10911808134` 并复查 48—52 五张截图。参与抽屉可用；项目页手机标题被操作按钮挤窄，随后仅补 650px 以下项目操作区单独换行，不修改权限、数据、测试或 CI 设置。修正后的最终检查另行补录。

下一切片转入 05-02 项目资料与修订，再接入按权限选材；见同一 [22](22-next-delivery.md)。完整状态/标签/attention 筛选、持久排序、私有选择性共享、正式存储和真实模型联调仍未交付。

## 04-01 任务真人负责人改派历史增量

接续前已核对并 squash 合入 PR #4：`d86a631`。其最终 [只读 CI 36249519976](https://github.com/ntygod/HEXU/actions/runs/36249519976) 为 204/204 工程测试、45/45 Chromium 流程及格式/类型/构建通过。本批从该合并基线推进 04-01，原 102 项保持 **2 完成 / 61 部分 / 39 未实现**；负责人切片不代表参与者/完整任务模型已完成。

- 项目可见任务头部接入真人负责人选择、不可变改派历史、分页、成员退出/只读提示。可编辑项目成员可以改派，目标必须同时是当前空间/项目 edit/manage 成员；私有任务拒绝改派，不把责任关系用作访问授权。
- Task.ownerUserId 与 Task/Run.createdByUserId 分开，新任务/执行在创建时记录真实操作者；迁移 11 对旧记录仅补 null，不从当前负责人猜历史创建者。旧 Run/节点所有者/账户/目录/原生历史不转交。
- 负责人修订、改派记录、preview/node 待接续暂停、任务范围事件和回执同事务。改派不停止已运行执行、不创建 Run、不重写冻结材料；未许可派发继续以原材料通过任务修订检查，失配拒绝，不自动重发。改回原人或重放旧回执不复活旧安排。
- W1 抽屉固定编辑基线；冲突保留选择但需明确核对，回执不确定仅确认原请求。关闭/刷新/身份空间变化和撤销编辑权清除本地选择；页面仍保留无关讨论草稿。只读成员可查看记录，不能改派。

本地 Linux / Node 24.21.0 完整 `npm run check` 已通过：**215/215 工程测试、0 失败/取消/跳过**，前后端类型和生产构建通过。原 204 条保留，新增 11 条覆盖严格身份输入、真实账号/成员权限、并发/幂等、事务故障、旧 SQLite 迁移重启、双类 Operation 暂停及节点发起者/授权保持。首次整套命令被工具时限中断，出现未完成的 node-executor 文件级测试，未记为通过；后续不受该调用时限中断的完整命令退出码为 0，未修改/跳过原用例或配置重试。

新增 4 条浏览器流程，共 49 条，最终功能 CI 已全部通过（见下方记录）；此前失败与修正分别保留，不将失败轮次计为通过。本地首次导航被 Chromium 的 `ERR_BLOCKED_BY_ADMINISTRATOR` 策略阻断，1 条未进入页面、2 条未运行；停止本地尝试，不绕过策略。使用与边界见 [任务改派](../engineering/task-assignment.md)。下一项为参与者关系及负责人/参与者筛选，见 [22](22-next-delivery.md)。真实模型联调和跨电脑部署限制不变。

### 04-01 浏览器首轮与修正

[首轮完整 PR CI 36253120999](https://github.com/ntygod/HEXU/actions/runs/36253120999) 检查合并预览 `1c3412bc5f60adbfe527a3ec4ed54c09323b40b8`（功能头 `7c2ba926`）：格式、类型、构建和 **215/215 工程测试通过**，浏览器实际 **47 通过 / 2 失败**。此前旧头的检查被后续提交按现有 CI 并发规则取消，不能计为通过。已下载产物 `10909748795` 并核对 SHA-256 `6d09878401d9a5b8cadffb7f2dd8338d1d6a49183448aaf3d6734eefec8478a8`，读取失败页面快照与日志。

手机宽度的旧后代选择器隐藏了负责人区域的所有文字按钮，导致改派入口不可达；现限制为原布局的直接子控件，保留手机改派与查看记录入口。另一个新用例尚未进入改派即触发认证限流：原浏览器启动脚本把 4311/4312 的两个独立认证应用放在同一进程，底层内存计数相互影响。现用独立子进程保持各服务原认证规则和完整账号流程，不修改生产限流、不换 IP、不添加自动登录重试。测试服务仍仅使用固定可丢弃库和虚构认证配置，受父进程生命周期管理。 本地真实 HTTP 冒烟检查确认：节点服务前 20 次登录允许、第 21 次返回 429，独立团队服务的首次登录仍成功；父进程退出后 4310/4311/4312 全部关闭。

复查还为抽屉辅助按钮明确设置 type=button，增加比较后仍需保存、重试历史不会提交的断言；对已移出空间的负责人使用任务范围已记录姓名或未知标记绘制头像，不回退成默认示例成员。新增成员退出断言。全部旧用例保留，不增加超时、重试或跳过来隐藏失败；修正后的完整 CI 结果单独补录。

### 04-01 第二轮核对

[第二轮 PR CI 36254066500](https://github.com/ntygod/HEXU/actions/runs/36254066500) 检查合并预览 `8609b62030c6b702b3e1d5047e83f123aec91299`（功能头 `94e608f8`）：格式、类型、构建和 **215/215 工程测试通过**；浏览器 **48 通过 / 1 失败**。手机流程与独立认证完整流程均已越过先前阻断点；唯一失败是新成员用例对原生 option 使用了不适用的通用禁用断言。已下载产物 `10910296055`，核对 SHA-256 `4ff03b0de9deab03b1a8d78cebfd2aa76d2d3fe7911c51912e734bd660b79b6c` 并检查页面快照：离开项目的成员确为 disabled option，保存按钮也禁用。

将这一断言改为核对原生 DOM disabled 属性，同时验证保存按钮禁用；保留随后真正选择可用成员并保存、确认创建者不变及没有执行的断言，不修改业务权限。截图复查同时整理新抽屉的字段纵向排布和底部按钮间距，复用已有 W1 field/dialog-footer，不另建样式体系。第二轮的 48/49 不计为全部通过；修正后的最终全量结果见后续记录。

### 04-01 最终功能检查

[完整只读 PR CI 36254895334](https://github.com/ntygod/HEXU/actions/runs/36254895334) 已完成且全部通过，实际检查合并预览 `93761384ef10581952113a5f41decf0d004417da`，对应功能头 `9479546e81f00980196222167b0d39a4b19f817b`。已读取完整日志：锁文件安装、格式、双端类型与生产构建通过；**215/215 工程测试、49/49 Chromium 浏览器流程通过**，无失败、取消、跳过或重试。原 204 工程 / 45 浏览器全部保留，新增 11 / 4 条。

已下载产物 `10910456359` 并核对 SHA-256 `93304e1d334fb39a5eb9fbb7a1aa9738f57380bc312936c89e775316bf502598`。实际解析报告为 total 49 / expected 49 / unexpected 0 / flaky 0 / skipped 0；复查 44—47 深色、浅色、390px 手机和双页冲突截图。字段纵向布局、底部固定操作、失效成员不可选择、必须显式保存及撤权清理均包含在通过结果内。浏览器通过来自仓库 Linux CI，不将本地被策略阻断的尝试计为通过。

功能树 `69b006249de09f3f47c0d6719d1ba7a3ee350d25` 已与本地 216 个跟踪文件复算完全一致。此后的结果补录仅修改本页，不修改功能、测试、配置或依赖；最终合并提交和只读 CI 另见 PR #5 检查与合并记录。临时开发传输工作流从发布树删除，以 squash 合入，不带入临时工作流历史。任务参与者、共用筛选及真实模型联调仍未交付；原 102 项统计不变。

## 03-06 项目归档/恢复历史增量

从 `567427c`（PR #3）继续，在既有项目 ID、修订和设置抽屉上实现归档/恢复。原 102 项仍 **2 完成 / 61 部分 / 39 未实现**；保留原工作项及其远程/完整配置剩余范围。

- 归档/恢复要求项目管理权、严格语义输入和当前修订。迁移 10 为修订历史增加归档状态；项目状态/历史、等待安排暂停、未许可派发取消、通知与回执原子提交。无变化不制造修订，旧回执不再次改变项目或停止新工作。
- 项目归档立即把 preview/node 待接续 Operation 改为 needs_attention，固定材料保留；即使马上恢复也不会自动创建 Run。未获许可的节点派发取消并返还尚未使用的所选要求；已许可/未知状态不伪造终止，也不释放占用。
- 已获许可/已启动执行由管理者明确选择保留或请求停止有操作权的任务；影响摘要不泄露私有任务。停止是请求，不是确认。直接 Run、原生恢复、接续最终事务、节点一次性许可及 preview 异步准备后均复核状态。
- W1 归档影响/恢复沿用设置抽屉，冲突不覆盖草稿，未知回执复用原请求。项目列表提供已归档筛选，侧栏和任务页标识归档；SSE 关闭新执行面板并禁用重配，仍保留停止、人工讨论/完成和成果，不删除代码或旧记录。

本地 Linux / Node 24.21.0 完整 `npm run check` 已通过：**204 条工程测试、0 失败、0 跳过**，前后端类型和生产构建通过；格式与 diff 检查通过。新增 13 条覆盖归档状态/修订、权限、私有摘要、事务回滚、未许可/已许可处理、双类 Operation、SQLite 重启、未知锁和真实节点 HTTP/进程停止。浏览器新增 4 条，共 45 条，修正下述标签问题后已在本批只读 CI 全部通过；没有绕过已有 Chromium 回环管理策略，也未把旧截图当作新功能验证。原 191 条工程、41 条浏览器用例保留；所有原生测试使用虚构 Key/协议替身，不调用真实模型。

### 03-06 浏览器首轮修正

[首轮 PR CI 36248277736](https://github.com/ntygod/HEXU/actions/runs/36248277736) 的 204 条工程测试、类型、构建和格式均通过；浏览器实际为 **42 通过 / 3 失败**。三项均在“已启动执行的处理”下拉框的精确标签定位处超时，尚未走到归档提交，不能计为流程通过。已下载并核对失败产物 `10908521717`，查看页面快照和截图；`4a362b4` 给该选择控件补充明确的可访问名称。保留原定位与全部断言，没有跳过、删除用例、增加超时或启用重试。

### 03-06 完整只读 CI

[PR #4 CI 36248899040](https://github.com/ntygod/HEXU/actions/runs/36248899040) 已完成并实际读取日志：检查的是合并预览 `53796b6e8df07eac823990671f9200a2e6e29e0d`，对应功能头 `4a362b4daec7e38a691e92a8bc25439c7621fa4c`。锁文件安装、格式、双端类型、生产构建、**204/204 工程测试、45/45 Chromium 浏览器流程通过**，无失败、跳过或重试。原 191 工程/41 浏览器用例保留，新增 13/4；协议替身不代表有效账户模型联调。

新增浏览器流程实际走通归档保留活动执行、筛选及人工协作、恢复不重跑、两页冲突和丢失回执重放、实时关闭新执行面板但保留讨论草稿，以及独立节点暂停接续、实际停止确认和未提交文件保留。已下载产物 `10908358302` 并核对 SHA-256 `1cb8332b823d626aa624e328013aea20187bb3441e2e12c26bffdd24abca7ce8`；报告统计 45 expected / 0 unexpected / 0 flaky / 0 skipped。复查新增归档选项深色、归档任务、恢复浅色/390px 手机和节点归档五张截图；使用虚构数据，不入 Git。

功能树 `75b78c2e40d1375228781904335422333d12eceb` 与本地完整复算一致，仅 37 个功能/测试/文档文件变更；临时源码及发布工作流已移除，常规 CI 仍为 contents:read。本节为检查后补录，不改变已检查的功能源码；最终文档提交的 CI 以 PR 检查和合并记录为准。

用法与明确边界见 [项目归档](../engineering/project-archive.md)。下一项为 `04-01` 任务负责人/参与者，仍不转移执行账户、现场或原生会话。

## 03-04 项目设置历史增量

从 `ba69b0c`（PR #2）继续，原 [主分支 CI 36243583789](https://github.com/ntygod/HEXU/actions/runs/36243583789) 已完成且通过。本批实现 `HX-DEV-03-04` 的项目基本信息编辑切片；102 项仍为 **2 完成 / 61 部分实现 / 39 未实现**，没有将包含归档、仓库引用等剩余范围的工作包提前完成。

- W1 项目设置抽屉可修改名称/说明、清空说明、查看分页修订；明确显示尚未接入归档/恢复和仓库引用。已有 Task/Run/Operation、成果、成员权限和节点授权不被重建或改写。
- `PATCH /projects/:id` 严格接受 expectedRevision/name/description。team-local 需要项目 manage，空间 owner 不越权；重放前和事务内均重新检查权限。过期修订返回 409，无内容变化不制造新修订。
- SQLite 迁移 9 增加项目修订记录和项目作用域通知。修改、历史、outbox、幂等回执原子提交；已有库只记录升级时已知快照，旧作者/日期未知保持 null。历史读取和事件投递遵守当前项目权限。
- 编辑基线在打开时固定，SSE 不静默替换草稿或提高保存基线；冲突需先查看最新内容并明确选择。回执丢失可复用原操作确认，不重复保存。项目草稿只在抽屉内存中，关闭/刷新/切换身份空间/撤销管理权会清除。

本地 Linux / Node 24.21.0 的 `npm run check` 已通过：**191 条工程测试，0 失败、0 跳过**，前后端类型与构建通过；新增 7 条覆盖输入、权限、并发、回执重放、无变化保存、回滚、旧库重启及项目事件范围。格式检查与 diff 检查一并执行。新增 4 条浏览器流程已在下述 PR CI 中通过。

本地 Chromium 首次页面访问返回 `ERR_BLOCKED_BY_ADMINISTRATOR`，已停止该浏览器尝试（1 个用例被环境阻断、2 个未运行），不修改环境策略或暴露服务；浏览器结果以仓库 Linux CI 为准。没有使用真实模型 Key，也没有将此前双工具协议结果记作真实模型验证。

[PR #3 只读 CI 36244886941](https://github.com/ntygod/HEXU/actions/runs/36244886941) 已完成。检查的是合并预览 `292a4b227da7d5f8fa21d9b1af9b95b5e669545d`（功能分支 `5dfbc596064669bf61b0f7c6c39a5fe09d0f46ac`，源码树 `4040a41a88dc8895faf7a8c927ec0e6c962fc75b`）。实际日志确认：锁文件安装、格式、前后端类型、构建、**191 条工程测试和 41 条 Chromium 浏览器流程全部通过**，无失败、跳过或重试。新增浏览器覆盖保存/刷新/历史、两页并发与草稿选择、丢失回执确认和管理权撤销清理；原有 37 条流程保留。

已下载浏览器产物 `10906629195` 并核对 SHA-256，复查项目设置深色、浅色、390px 手机及冲突比较四张截图。截图使用虚构测试数据，不替代断言，也不入 Git。最终功能树仅 20 个功能/测试/文档文件变更；临时发布工作流已移除，正常 CI 权限未改变。本段为检查后补录，不改功能代码。

03-04 当时的下一切片为 03-06，现已在页首记录本机交付；当前计划见 [22](22-next-delivery.md)。

## E2c2 历史增量

本轮接手 `0139ce2`（W1 合并后），推进 `08-03`，联动 `08-01/05、06-06、10-03、11-02`。原 102 项仍为 **2 项已完成、61 项部分实现、39 项未实现**：有界 Claude 恢复代码路径已实现，不将真实模型未联调的整包标为完成。

| 能力            | 实际范围                                                                                                            |
| --------------- | ------------------------------------------------------------------------------------------------------------------- |
| 双工具私有会话  | Claude/Codex 均由本人节点本机 retainSessions:true 授权；默认新会话，Claude/preview 显式 no-session-persistence      |
| Claude 显式恢复 | 节点生成 UUID；首次 --session-id，后续只 --resume；隔离 HOME/配置/项目目录，不导入个人订阅历史                      |
| 本地绑定        | 控制地址/节点/项目/任务/目录身份、工具路径/版本/策略/模式、精确 Key HMAC；恢复前比对私有文件内容指纹                |
| 协议和终态      | 核对 init UUID/cwd/model/dontAsk/tools/MCP 及 assistant/result 一致性；成功且确认进程组结束、历史同步落盘后才 ready |
| 失败和停止      | 缺失/改动/到期/链接/错误 ID/模型/权限/异常生命周期拒绝；无静默新建或付费重试；unknown 保锁                          |
| W1 任务入口     | 默认新会话，选择时按实际 Claude/Codex 命名，区分恢复请求、保留引用和已恢复；每次仍创建新的 Run                      |
| 本机管理        | 同一 native-sessions / forget-native-session 管理两工具；各 32 记录、7 天恢复期限；Claude 检查 4096 项/64 MiB/16 层 |
| 兼容性修正      | 官方 2.1.283 的 max-turns 不在 help 中显示，原探测会误判；仅对该精确版本的这一参数作例外，其他受限参数仍严格检查    |

原生历史和 provider UUID/账户指纹不上传；预览只有新增材料。仅允许最新成功来源、同范围、本机账户未变化的显式恢复。wait/request_stop Operation 保持新会话；Task 完成、Run 结束与 Operation 创建成功分开。

## 未完成与限制

**有效账户真实模型生成及成功原生恢复仍未联调。** 协议替身回归、官方无模型检查分开记录，不能由测试数量或 UI 截图推出真实账户可用。文件工具策略不是 OS 沙箱，私有历史也不是已脱敏共享输出。Key 轮换即使同账号也需新会话。初始化/结果身份检查不能证明模型在发送 init 前未消费请求，不承诺异常恢复零费用。

同机回环、本人节点执行、Linux 工程回归边界不变。实时输入/steer、失败/中断历史自动对账、跨账号/跨节点恢复、他人节点授权、远程 TLS/WSS、PostgreSQL、Windows 原生执行、系统凭证存储、远程 diff/终端/预览及协助/接手/并行仍未完成。macOS 未验证。项目归档/恢复已补齐本机切片；下一项见 [22](22-next-delivery.md)。

## E2c2 本轮工程记录

本机 Linux / Node 24.21.0：已完成整套工程回归并追加非保留会话身份检查；最终数字以本节后续 CI 记录为准。新增覆盖 Claude 默认不保留、明确 UUID、重启恢复、账户和权限、文件缺失/篡改、链接/过期、异常重启、init/result/模型错配、无落盘历史、停止和清理。原用例未删除、未跳过，也未增加重试。

官方 Linux x64 Claude Code `2.1.283 (Claude Code)` 已执行 `check:claude-protocol`：version/help 与不存在 UUID 的显式恢复拒绝通过，结果报告 0 轮次、0 API 时长、0 费用；未提供 Key、未认证、未调用真实模型。下载固定发布资产并核对 SHA-256 `db404a91bec8baffb53463166fdc8bf579208a527d60afc2d984d7507dc0c2f9`。它不验证已有真实历史的成功恢复。

本地整套浏览器尝试被 Chromium 管理策略以 `ERR_BLOCKED_BY_ADMINISTRATOR` 阻止访问回环页面，**不是本地浏览器通过**；同一 37 条流程转到 GitHub-hosted Linux 执行。本地首次完整命令被工具调用时限中断，随后不中断执行完成；中断记录不作成功依据。实际 CI 结果在通过后追加，不提前标记。用法见 [Claude 会话](../engineering/claude-sessions.md)。

### E2c2 完整 Linux 开发检查

[开发检查 36242636683](https://github.com/ntygod/HEXU/actions/runs/36242636683) 已通过；实际被检查的功能源码提交为 `91ba8483f4c9bb5920db625ab5410095982b334f`，不是触发工作流的临时传输提交。Node 24 全新锁文件安装、格式、双端类型与生产构建通过；**184 条工程测试、37 条 Chromium 浏览器流程全部通过**，无失败、跳过或重试。新增 2 条 Claude 浏览器流程实际启动独立节点 CLI，覆盖授权保留、节点重启后的显式恢复、清理后明确拒绝和用户主动新开会话；原有 35 条流程保留。

同一检查还下载并校验官方 Claude Code 2.1.283 固定资产，执行 `check:claude-protocol` 通过；仍仅验证无模型的参数/缺失会话拒绝，不是有效账户生成或成功历史恢复。临时传输工作流仅用于此开发分支，合并前移除，正常 CI 保持只读。

## E2c1 历史工程记录

本地 Linux / Node 24.21.0：**175 条工程测试通过，0 失败、0 跳过**；格式、双端 TypeScript、前后端构建通过。新增恢复取消、私有历史跨进程、精确账户绑定、错 ID/模型/权限、缺失/清理、过期/链接和异常重启保护。所有旧用例保留。

[E2c1 完整开发检查 36229301019](https://github.com/ntygod/HEXU/actions/runs/36229301019) 已在 GitHub-hosted Ubuntu / Node 24 通过。工作流应用并格式化后的实际检查源码为 `d0ea91c8fd8c9f7947d9812ff4e11ffac5ce4c19`，不是触发工作流的临时传输提交。已下载其源码快照，与本地应用、包、测试、脚本、文档及配置共 157 个文件逐一核对一致（不含临时工作流、传输和生成文件）。

| 检查                                                | 实际结果                                                                                           |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 锁文件全新安装、格式检查                            | 通过                                                                                               |
| 双端 TypeScript、前后端构建                         | 通过                                                                                               |
| 领域、存储、API、认证、节点、原生协议替身与会话测试 | 175 条通过，0 失败、0 跳过                                                                         |
| Chromium 浏览器流程                                 | 34 条通过，0 失败、0 不稳定、0 跳过                                                                |
| 官方 Codex 0.157.0 无模型兼容性                     | initialize、持久会话受限 config/read、缺失 thread 的 read/resume 拒绝通过；未认证、未发 turn/start |
| 浏览器产物                                          | 已下载并读取报告，检查原生恢复工作区及手机深色界面                                                 |

新增 2 条浏览器流程实际启动独立 CLI，执行本机保留授权、首次会话、节点进程重启、显式恢复、同一私有历史引用与新 HEXU Run、清理后拒绝恢复，以及用户再次明确选择新会话后成功执行。协议替身的私有历史用于核对恢复路径，不能冒充真实提供方会话结果。既有 32 条浏览器流程全部保留。

### 官方程序检查的明确边界

检查下载官方 `rust-v0.157.0` 的 `codex-package-x86_64-unknown-linux-musl.tar.gz`（资产 ID `587334214`），同时核对 GitHub 发布摘要与实际文件 SHA-256：

`042f851ea3fc1083c45157520520944fc790632b53ebc580fc98eaca55862a25`

完整 package 布局中的 `bin/codex --version` 实际返回 `codex-cli 0.157.0`。`scripts/check-codex-protocol.mjs` 在隔离 HOME/CODEX_HOME 和临时 Git 目录检查配置及不存在会话的接口拒绝行为，没有账户认证或模型调用。**这只证明被检查版本的无模型配置/接口路径，不证明有效账户生成、已有真实会话恢复或其他版本兼容。**

### 检查中修正的问题

早期开发工作流使用了不适用的单文件发行资产名称/压缩格式，在下载步骤失败，未据此记录官方程序通过。改为实际发布的完整 package、固定资产 ID 和摘要后执行成功。浏览器曾有一项失败：通用禁用断言未正确表达 option 自身的 disabled 属性；已改用明确 DOM 属性检查，并增加真正执行用户主动新会话、核对新的会话引用及启动次数的断言。没有删除失败用例、跳过或配置重试来掩盖结果。

本地浏览器受环境回环访问策略限制，不作本地浏览器通过声明；上述浏览器结果来自仓库 CI。截图不替代功能断言。主分支最终结果以对应提交的只读 CI 为准。

上一轮 `73ef5c7` 的 166 条工程测试和 32 条 Chromium 流程仅作历史基线。临时开发传输和写入式工作流不进入 main，主分支只读 CI。用法见 [Codex 会话](../engineering/codex-sessions.md)，下一步见 [22](22-next-delivery.md)。

## 设计与客户端定稿补充（2026-09-26）

本次先同步主分支 `e8f0511`；该提交的[只读 CI 36229789957](https://github.com/ntygod/HEXU/actions/runs/36229789957) 已完成且通过。随后仅固化设计、客户端决定与开发指引，没有修改应用业务代码。

- [Workbench W1](../design/README.md)：暗色青色任务一体化工作台、浅深色与密度 tokens、面板／输入／状态规则、Figma 求助与交接信息结构的采用范围。
- [ADR-0008](../engineering/adr-0008-client-surfaces.md)：桌面优先、个人无需团队服务器、可选团队服务与 Web 协作入口；共用 UI、契约与领域，Runner 保持独立。Electron／Tauri 尚未选定。
- 根 AGENTS.md 增加必读入口与边界；同步旧视觉、仅 Web 和初始技术建议中的冲突描述。原 102 项及 **2 完成 / 61 部分实现 / 39 未实现**的统计不变。
- 归档可编辑参考片段与独立浏览器 HTML。演示仅使用虚构数据；生产样式迁移、桌面安装和其中未交付的业务功能继续标未完成。

本地 Windows 使用 Node 24.19.0 执行 `npm run format`；新增参考 CSS／HTML 片段经 Prettier 3.6.2 格式化，片段 JavaScript 语法检查通过。相对文档链接和 102 个原工作项 ID 已核对，主要动作颜色配对已计算对比度。独立导出参考已在浏览器打开，检查浅色切换、要求保存／队列抽屉，未发现控制台错误。以上是文档与演示检查，不是应用端到端测试或真实模型验证；本次未重复运行完整业务测试。

## W1 前端重建任务补充（2026-09-26）

用户选择先重排详细任务，本次仅更新计划。新增 [23｜W1 前端重建与旧 UI 清理](23-workbench-rebuild.md)，列出现有 UI 的替换／保留范围、9 个未开始的 W1 子任务、原工作项映射、交付顺序与删除条件。布局、组件和样式可以重新开发，真实请求、身份权限、事件、输入与接续行为须由新页面接管；旧呈现代码在接管后删除，不先清空应用。

已同步根 AGENTS.md、设计入口及 02／10／19／22 工作包。原 102 项的行内容、ID 和完成状态均保持不变；没有删除 UI、修改业务代码或新增桌面能力。Node 24 下 `npm run format` 执行通过且源文件未变化；检查相对链接、9 个 W1 标签和原父任务映射均通过。纯计划改动未重复运行应用测试。

## W1-01/02｜共享视觉基础与应用壳层

本批从 `4663c24` 开始，在隔离工作树推进 02-01/02/03，联动 02-06。当前业务仍是 E2c1；本批没有新增原生工具、远程或桌面宿主能力。

- W1 数值进入 `packages/ui/src/tokens.css`，设计目录引用同一来源；共享按钮、输入、状态、弹层、抽屉、空状态和通知样式集中到 UI 包。新访客默认深色／紧凑，沿用已有 `hexu-theme`，新增密度偏好，认证前后都生效。
- 新顶栏、导航轨和可折叠导引栏接入现有工作台、项目、任务、成果及设置。导引栏内容只使用已过滤的 Workbench 数据，桌面收起偏好按模式／用户／空间隔离；窄屏临时展开可用 Escape 关闭并返回焦点。
- 命令入口复用真实任务搜索和现有新建任务／导航动作。保留原 URL、身份切换、权限撤销、事件與执行流程，移除旧 App 外壳及对应 CSS，不保留第二套运行界面。
- 原任务页、输入／接续、身份与管理页面的深度重建留给 W1-03—09；过渡变量别名有明确消费者，不把本批记作全量 W1 迁移。

实现提交 `da7e8c7` 的 [Linux CI 36233662440](https://github.com/ntygod/HEXU/actions/runs/36233662440) 已通过锁文件安装、格式、双端类型检查、构建、**175 条工程测试和 34 条 Chromium 浏览器流程**，0 失败、0 跳过。已下载浏览器产物并检查桌面工作台与手机页面。原流程继续验证身份隔离、成员撤权、节点、输入、等待接续与 Codex 恢复；协议替身通过仍不代表真实模型联调。后续补充明确的浅深色切换断言，最终分支结果以 [PR #1](https://github.com/ntygod/HEXU/pull/1) 的只读 CI 为准。

本地 Windows / Node 24.19.0 下格式、双端类型检查与构建通过；使用独立 SQLite、关闭 native 的预览服务走通命令新建、主题／密度刷新保留、390px 导航展开／Escape 与无横向溢出。检查中修正命令弹层初始焦点，使打开后落在搜索输入。Windows 自动浏览器检查未完成：默认端口占用后使用仅更换端口的临时副本，但缺少本机 Chromium；上述完整回归来自 Linux CI，未把启动失败或手动截图记为自动测试通过，未删除、跳过用例或增加重试。

W1-01/02 在本批范围内完成；下一批从 W1-03 任务工作区开始，再推进 W1-04 输入与 W1-05 接续。剩余页面与旧变量清理、正式个人客户端、桌面安装及远程部署继续保留未完成状态；原 102 项统计不变。

## W1-03—09｜整套呈现层重建

用户确认连续完成整套新 UI，并删除旧页面、布局和样式。沿用 W1-01/02 基础，现有行为迁入新页面，不将新旧混搭作为最终交付。业务和原 102 项编号保留。

W1-03/04/05 已进入新组件：`task-workspace`、`discussion`、`prompt-bar` 与 `execution.css`。任务头分别显示业务／执行／实际代码来源；过程、成果双栏与可折叠面板、键盘可调宽度、上下文／执行历史抽屉可用。新记录仅在用户位于底部时跟随，阅读历史时显示返回最新入口。讨论与下一轮输入分开；队列抽屉保留作者权限、修订、撤回与使用状态。草稿仅存在当前身份／空间 Provider 的内存中，切换任务／关闭设置时保留，退出／切空间／撤权后清理，不写入浏览器存储，刷新页面不保留。

原生和节点配置按目录配置、会话方式、要求材料分组，接续安排与节点进度移入过程区。202 Operation、固定材料、取消、显式 Codex 恢复与历史继承提示仍使用原协议；未增加原生实时输入、节点 diff 或终端能力。已删除对应旧任务／消息／输入／执行 CSS；其余页面及通用旧样式随 W1-06—09 继续重建清理。

Node 24 下格式、类型检查通过，构建已通过任务／输入重建；包含最新接续布局的完整 CI 正在准备。原 34 条浏览器流程保留并适配队列抽屉，增加草稿、发送失败与历史阅读检查；在 CI 实际完成前不标全套迁移完成。

W1-03/04/05 的提交 `1f2a2d3` 已通过 [Linux CI 36237351685](https://github.com/ntygod/HEXU/actions/runs/36237351685)：格式、类型、构建、175 条工程测试和 35 条浏览器流程通过，0 失败、0 跳过。新增检查覆盖打开抽屉／切换任务后草稿保留、发送失败不丢草稿、历史阅读不被新记录抢走位置，以及布局刷新保留。

W1-06—09 已完成代码替换并通过整套最终回归：账号／团队采用新组件样式；工作台、项目、成果、资源与命令菜单从旧 App 中移出并重建。示例订单单独保留明确标签和 CSV 行为。旧 `styles.css` 整体删除，旧配色映射全部移除，新基础为 `foundation.css` 与各功能样式；没有新增页面 palette、桌面壳或生产示例数据。手机任务页切换讨论和代码／成果，输入在当前可视工作区内保持稳定。

本地 Windows / Node 24 的格式、双端类型与构建通过；检查所有运行 CSS 的变量引用，无未定义变量。手动检查了新工作台、手机任务输入、浅深色和成果面板切换，390px 无整页横向溢出。当前 SSH 直连测试机也已恢复；此前远端 smoke 针对 `3e6b352`，不计为本次完整重建版本通过。最终验证如下。

### W1 最终交付检查

功能提交 `43c066fd8e8a17af606973416a341e29d1b1882a` 的 [Linux CI 36238774668](https://github.com/ntygod/HEXU/actions/runs/36238774668) 全部通过：锁文件安装、格式、双端 TypeScript、构建、**175 条工程测试和 35 条 Chromium 浏览器流程**，0 失败、0 跳过。原流程未删除，未增加重试。检查保留真实权限／撤权、节点配对／执行、固定材料等待／取消与显式 Codex 恢复路径；这些原生步骤仍是协议替身，不代表有效账户模型生成或恢复已联调。

已下载本次 CI 的浏览器产物，检查桌面任务工作区、浅色团队设置、手机节点及输入区；本地也检查手机讨论／成果切换与主题。运行样式无未定义变量，相对文档链接有效，102 个原任务 ID 与 **2 完成 / 61 部分实现 / 39 未实现**统计保持。文档补录不改变上述功能源码。

**W1-01—09 在既有能力范围内全部完成。** 原 App 页面和旧样式表已退出运行，接口、身份权限、事件、停止、输入、Operation 与原生恢复流程由新界面接管。未借 UI 重建补造协助、接手、并行、终端、远程 diff、不可变成果版本、完整费用账本、正式纯个人客户端或桌面安装；它们继续属于原工作包。本轮交付与合并记录见 [PR #1](https://github.com/ntygod/HEXU/pull/1)。

后续 AI 接手从 [24｜接手指南](24-ai-handoff.md) 进入，下一主线仍是 [22](22-next-delivery.md) 的 HX-DEV-08-03 Claude 会话映射。README、AGENTS 与计划入口已统一，不再把已完成的 UI 重建列为待办；本次交接整理没有修改功能源码或原 102 项状态。
