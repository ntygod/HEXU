# 21｜当前实现进度

更新：2026-09-26（UTC+8）。阶段：**E2c1 Codex 节点私有会话与显式恢复**。产品 v1.1 / D1 不变。

## 任务位置与实际增量

推进 `09-02/05/06`，联动 `06-06、10-03、11-02`。原 102 项仍为 **2 项已完成、61 项部分实现、39 项未实现**。第 09 包没有整包完成，08-03 Claude 恢复本轮未实现。

| 能力 | 实际范围 |
| --- | --- |
| 可选保留 | 只有独立节点本机 Codex retainSessions:true 授权才使用私有持久 CODEX_HOME；默认与 preview 仍临时会话 |
| 原生协议 | 成功且确认退出后保存 thread/turn/model；恢复先 thread/read、再 thread/resume、独立 turn/start，不复制文本伪装恢复 |
| 绑定检查 | 同节点、控制地址、项目/任务、目录文件身份、工具路径/版本/策略/模式、精确 Key HMAC；服务端和节点分别核查 |
| 失败与取消 | 历史缺失、到期、异常/未知、错误原生 ID/模型/权限明确失败；引导取消不发送下一轮；无静默 thread/start 或重试 |
| 本机管理 | native-sessions 列表、确认后的 forget-native-session；7 天恢复期限、32 记录上限、准备/结束时有界文件检查；不自动删除过期历史 |
| 任务界面 | 新会话为默认；明确选择恢复和继承历史提示，恢复请求/保留引用/已恢复分开展示；原生历史不上传 |

每个恢复仍创建独立 HEXU Run，在同一 Task/目录执行。接续材料预览仅代表新增材料，原生恢复继承原历史。只有最新成功来源可用，恢复派发不等于模型成功；跨工具依旧新会话。已存在的 wait/request_stop Operation 保持新会话，不支持自动原生恢复。

## 未完成与限制

**有效账户真实模型生成及原生恢复仍未联调。** 代码/流程检查用明确协议替身、虚构 Key 和实际进程/HTTP/Git。上游官方无模型检查另记，不与真实账户成功混报。文件策略不是 OS 沙箱，原生私有历史不等于已脱敏的共享输出。Key 轮换即使同一账号也需要新会话。

同机回环、本人节点执行范围不变。Claude resume、实时输入/steer、失败/中断历史自动对账、跨账号/跨节点恢复、他人节点授权、远程 TLS/WSS、PostgreSQL、Windows、系统凭证存储、远程 diff/终端/预览、协助/接手/并行仍未完成。

## 工程记录

本地 Linux / Node 24.21.0：**175 条工程测试通过，0 失败、0 跳过**；格式、双端 TypeScript、前后端构建通过。新增恢复取消、私有历史跨进程、精确账户绑定、错 ID/模型/权限、缺失/清理、过期/链接和异常重启保护。所有旧用例保留。

新增 2 条 Chromium 流程定义，浏览器结果与官方无模型检查尚待具体 CI 登记，不能从测试定义推断通过。本地浏览器受环境访问策略限制，不作本地浏览器通过声明。上一轮 `73ef5c7` 的 166 工程测试/32 Chromium 流程只作历史基线。

临时开发传输和写入式工作流不进入 main，主分支只读 CI。用法见 [Codex 会话](../engineering/codex-sessions.md)，下一步见 [22](22-next-delivery.md)。
