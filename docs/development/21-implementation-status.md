# 21｜当前实现进度

更新：2026-09-26（UTC+8）。阶段：**E2b2 独立节点本人授权执行**。产品 v1.1 / D1 完整目标不变。

## 原任务位置

推进 `07-01/02/03/06` 联动 `06-04/05/06` 与已有 08/09 适配器、10 工作区。原 102 项为 **2 项已完成、61 项部分实现、39 项未实现**；这些条目本轮前后都是部分实现，数量不变不代表没有代码增量。完整范围和剩余项见 [19](19-work-items.md)。

## 实际交付

| 能力 | 本轮实现与范围 |
| --- | --- |
| 独立执行授权 | 默认仍只有摘要。节点本机 enable-execution 确认工具绝对路径、目录子集、模式、时间/费用限制。只有节点所有者能派发，不把项目成员可见性当执行权 |
| 原任务中执行 | 使用既有 Task/Run，新增 node provider；输入快照、派发、幂等与事件通知在一个事务保存；不会创建另一套节点任务 |
| 接单与启动 | 节点落盘 accepted 后确认接单；服务端一次性启动许可后才尝试启动，实际 spawn 事件才标运行。重复许可不能重复启动 |
| 实际工具进程 | 独立节点复用 Claude CLI / Codex App Server 受限适配器、节点本机 API key、隔离 HOME、只读/文件编辑。未开放 Shell、MCP、额外网络工具或账户兜底 |
| 输出与事件 | 有界状态/文本/终态先写本机日志，服务持久保存后 ACK；丢回应重发同一事件。已知凭证与本机路径脱敏，不上传 raw reasoning 或完整日志 |
| 停止与恢复 | 队列取消不冒充已运行；活动执行收到停止或通信失败即保守请求停止。只有确认终态才能释放占用；未知进程须在节点本机明确核对，不杀磁盘旧 PID、不自动重跑 |
| 目录占用 | 同一 OS 用户的 preview 与独立节点共用持久化目录注册表，根目录或父子目录重叠均互斥；进程状态不明时占用跨重启保留；拒绝把锁目录纳入可写授权根 |
| UI | 在任务内选择本人节点/目录/模式、查看材料与固定工具限额、确认共享输出和费用；执行卡区分派发/接单/许可/实际启动/结束，刷新保留 |
| 撤权 | 撤权后不再接命令或新输出；已绑定派发只允许排空事件序号并提交停止证据，不能借旧凭证恢复执行 |

任务完成与执行完成依然分开。关闭网页不会停止执行；服务或节点重启不会重放付费工作。退出账号不等于撤销设备，仍须明确撤销节点或停用执行。

## 尚未交付

两个服务模式仍仅监听回环地址，**本轮是同一机器上的独立节点进程，不是跨电脑远程团队部署**。服务不托管团队模型账户。指定其他成员使用节点、通用 AccessGrant、WSS/HTTPS 部署、PostgreSQL、系统凭证存储、Windows 支持仍未完成。

节点只支持新会话的单次执行；原生 resume/steer、运行中输入、节点跨工具继续、远程 diff/终端/预览、完整用量/原生引用尚未实现。没有临时协助、跨成员代码检查点接手或并行分支。

**有效账户下真实模型生成仍未联调。** 流程用真实进程和临时 Git 目录，但原生程序使用明确标识的协议替身与虚构 Key。Linux 已运行工程检查，macOS 未实测。文件工具限制不是操作系统沙箱，输出脱敏不是通用数据防泄漏系统。

## 工程记录

本地 Linux / Node 24.21.0：格式、类型检查、前后端构建和 **140 条工程测试**通过，0 失败、0 跳过。新增实际 HTTP、独立工具进程、事务回滚、授权范围、ACK/代次、队列取消、断线停止、撤权结算、未知日志不重跑、共享/重叠目录占用和凭证删除保护。

[E2b2 完整开发检查 36218970268](https://github.com/ntygod/HEXU/actions/runs/36218970268) 已在 GitHub-hosted Ubuntu / Node 24 通过。工作流应用并格式化后的实际检查源码为 `2b6050ea13761c83a9c9a35b07f9c624f769932a`，不是触发工作流的临时传输提交。已下载其源码快照，并与本地检查的应用、包、测试和脚本逐文件核对一致。

| 检查 | 实际结果 |
| --- | --- |
| 锁文件全新安装、格式检查 | 通过 |
| 双端 TypeScript、前后端构建 | 通过 |
| 领域、存储、API、认证、原生协议、节点与执行工程测试 | 140 条通过，0 失败、0 跳过 |
| Chromium 浏览器流程 | 26 条通过，0 失败、0 不稳定、0 跳过 |
| 浏览器产物 | 已下载并读取报告，抽查独立执行完成和手机深色运行页面 |

新增 3 条浏览器流程实际完成本机 EXECUTE 授权、独立 CLI 派发与文件修改、输出回传、刷新、重启不重跑、运行中停止，以及拒绝授权后不可执行。既有 23 条流程保留。原有只读成员的按钮名称断言随 UI 更新，禁止执行的断言仍保留，没有用跳过或删除失败用例掩盖结果。

复查增加了授权根不得包含持久化工作区锁目录的保护与回归；过大目录授权不能让 Agent 修改自己的互斥记录。该保护不跨越 OS 用户或外部 IDE。

本地 Chromium 访问回环被环境策略阻止（ERR_BLOCKED_BY_ADMINISTRATOR），不计为本地浏览器通过；浏览器结果来自仓库 CI。主分支最终结果以其对应提交的只读 CI 为准。

上一轮主分支 `6b70404` 的 119 条工程测试与 23 条浏览器流程为历史基线，不当作本轮结果。开发检查不调用真实模型，主分支保持只读 CI。临时开发传输与集成工作流不进入主分支。

## 下一步

独立节点执行已有本人授权的回环路径，但不以此放开公网地址。下一批先完善任务的下一轮要求与同目录继续，再补原生会话、明确他人执行授权及远程传输和正式存储边界；顺序见 [22](22-next-delivery.md)。本轮配置与恢复见 [节点执行说明](../engineering/runner-execution.md)。
