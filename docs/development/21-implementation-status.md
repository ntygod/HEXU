# 21｜当前实现进度

日期：2026-09-25。代码阶段：**E1a 本机原生文件工具接入**。产品 v1.1、开发计划 D1 的完整范围不变。

## 本轮新增

本机 Claude Code CLI 受限文件工具适配；显式 Git 根目录配置；必要参数探测；输入快照与连续 Run；JSONL 输出；持久化目录占用；真实进程组停止；重启未知状态保留和离线人工恢复；实际 Git 变更界面与原生资源页。

`apps/runner/src` 当前由本地控制服务托管，不是独立 daemon/WSS 节点。原生能力默认关闭；没有配置凭证时仍可使用原有模拟预览。

## 真实、替身与未完成

| 内容 | 状态 |
| --- | --- |
| E0 项目/任务/讨论/成果与 SQLite 数据流程 | 保留并通过本轮回归 |
| 子进程、Git 临时仓库、文件修改与停止 | 已由真实操作系统进程测试 |
| Claude JSONL/CLI 参数映射、错误和输出处理 | 已实现实验性切片；测试使用明确标识的协议替身 |
| 真实 Claude Code + 有效 API 账户调用 | **未执行**，没有提供方兼容或质量通过结论 |
| Codex、多用户身份、PostgreSQL、独立 Runner | 未实现 |
| 同工具原生恢复、实时输入、Bash、MCP、通用终端 | 未实现或本轮主动禁用 |
| 跨电脑接手、临时协助、并行代码分支、远程预览 | 仍待后续工作 |

CLI “已检测”只表示找到所需参数与本地 key 配置，不等于凭证有效。工具实际联网、模型可用性、计费与本机权限行为仍需使用者以有授权的环境联调。

## 本轮工程结果

本地环境：Linux，Node 24.21.0，锁文件依赖。已实际完成格式检查、双端 TypeScript、44 条领域/存储/API/原生运行用例和生产构建。

[开发分支完整检查](https://github.com/ntygod/HEXU/actions/runs/36153889156)在 GitHub-hosted Ubuntu、Node 24.21.0 完成；使用锁文件全新安装。检查后的源码提交为 `d06d444247976120b24047be307eace41ff8139a`。

| 检查 | 实际结果 |
| --- | --- |
| npm ci 与格式检查 | 通过 |
| 服务端、网页 TypeScript | 通过 |
| 领域、存储、API、原生进程与 Git | 44 条通过，无跳过 |
| 服务端与网页构建 | 通过 |
| Chromium 浏览器流程 | 13 条通过，0 失败、0 不稳定、0 跳过 |
| 运行截图 | 已取得资源页、原生开始面板、真实 Git 变更页及原有页面截图，并检查关键页面 |

新增后端用例覆盖显式确认、能力参数、输出过滤、目录/符号链接边界、真实 Git diff、stdin/超时/停止、缺工具或凭证、协议替身实际写文件、重复请求不重复启动、跨任务目录互斥、完成任务时停止原生、缺少 result 不判成功、重启占用保留。

新增浏览器流程覆盖资源页、原生开始与实际文件变更、手机宽度面板。第一次运行有一条表单标签定位失败，随后补充明确的可访问标签并重新运行，未通过跳过用例消除失败。

本地浏览器导航被环境策略阻止，因此浏览器结果来自仓库 CI，而非本地运行。所有原生工程测试强制使用临时仓库、协议替身和虚构 key，覆盖继承环境，不会调用真实模型或使用公司账户。截图属于运行中的测试界面，不是实际模型代码质量证明。

临时源码传输、开发依赖打包和写入式 staging 工作流不进入主分支；主分支继续使用只读权限常规 CI。主分支最终运行结果以对应 Actions 记录为准。

## 工作包进度变化

| 包 | 当前增量 | 仍缺少 |
| --- | --- | --- |
| 06 执行器 | 本机目录、Git、受管进程组与恢复边界 | 独立 CLI 配对、WSS、spool、多节点、Windows |
| 07 执行事件 | 原生与 mock 分离，原生事件和目录占用持久化 | 网络派发、完整交互输入/授权、远程恢复 |
| 08 Claude Code | bare/restricted 文件工具路径、结果解析与停止 | 真实提供方联调、原生 resume、实时输入和更丰富能力 |
| 09 Codex | 保持待接入，不冒充可用 | 原计划中的原生实现 |
| 10 工作区 | 原生选择、Git 变更和输出页 | 全文件编辑、终端、外部 IDE 与通用预览 |
| 11 继续 | 同目录新 Run + 最近工作记录；保留前驱 | 完整工具间接续、临时协助、完整项目资料上下文 |
| 03 / 17 | 保留单用户本地模式 | 认证、正式团队部署、远程节点 |

不是把上述整个工作包标为完成，也没有把产品的内部评估重新做成用户流程。

## 使用与下一步

见 [本机原生说明](../engineering/native-execution.md) 和 [本轮架构选择](../engineering/adr-0002-local-native-runtime.md)。继续沿统一 Task/Run 模型推进 Codex 适配和多人基础；不会因为本机 CLI 可以运行就开放公网。

E0 的历史基线为 `9786e8b422698292ac5fc2939817b4d8ce9d54e6`，29 条后端用例、10 条浏览器用例记录见其 Git 历史。本轮未更改许可证、仓库可见性或部署外部服务。
